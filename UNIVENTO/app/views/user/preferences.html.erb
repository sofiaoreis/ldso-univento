<div class="page">    
          <h2>Para teres uma pesquisa ainda mais personalizada, </h2>
          <p>responde ás seguintes questões.</p>
 	
  <%= form_for @user, :url => "savePreferences?id="+@user.userID.to_s , method: :post do |u| %>

    <div class="ambiente">
    	<p>Que tipo de ambientes te agrada mais?</p>
        <% @ambianceTags.each do |tag| %>
          <%= check_box_tag 'ambianceTags[]', tag.tagsID, @tag == tag %>
          <%= tag.name %>
        <% end %>
    </div>

     <div class="musica">
    	<p>Os teus estilos de música preferidos:</p>
        <% @musicTags.each do |tag| %>
          <%= check_box_tag 'musicTags[]', tag.tagsID, @tag == tag %>
          <%= tag.name %>
        <% end %>
    </div>

    <div class="tipos">
     	<p>Estás à procura de eventos em que áreas?</p>
        <% @categories.each do |category| %>
          <%= check_box_tag 'categories[]', category.categoryID, @category == category %>
          <%= category.name %>
        <% end %>
    </div>

    <div class="tipos">
     	<p>És uma pessoa que prefere eventos mais diurnos ou noturnos?</p>
      <label for="eventTime"></label>
        Diurnos<%= radio_button_tag 'eventTime', "dayTime", @eventTime == "dayTime", :checked => false %><br>
        Noturnos<%= radio_button_tag 'eventTime', "nightTime", @eventTime == "nightTime", :checked => false %><br>
    </div>

    <%= u.submit 'Submeter', :class => 'btn btn-primary active' %>
  <% end %>

  <br><%= link_to "Back", :back %><br>

</div>
       

   
   

     

<script>(function(d, s, id) {
  var js, fjs = d.getElementsByTagName(s)[0];
  if (d.getElementById(id)) return;
  js = d.createElement(s); js.id = id;
  js.src = "//connect.facebook.net/pt_PT/sdk.js#xfbml=1&version=v2.5&appId=548043568677408";
  fjs.parentNode.insertBefore(js, fjs);
}(document, 'script', 'facebook-jssdk'));</script>
